<resources>

    <!-- Base application theme. -->
    <style name="Theme.Biologer.Main" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorPrimary</item>
        <item name="iconColor">@color/icon_color</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/Widget.Biologer.ActionMode</item>
        <item name="actionModeBackground">@color/colorPrimary</item>
        <item name="actionMenuTextColor">@color/white</item>
    </style>

    <!-- Toolbar text color. -->
    <style name="Theme.Biologer.Toolbar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Toolbar theme. -->
    <style name="Theme.Biologer.Toolbar" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="titleTextAppearance">@style/Theme.Biologer.Toolbar.TitleText</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/black_on_light_white_on_night</item>
    </style>

    <!-- ActionBar for Contextual Action Mode -->
    <style name="Widget.Biologer.ActionMode" parent="Widget.AppCompat.ActionMode">
        <item name="background">?attr/colorPrimary</item>
        <item name="titleTextStyle">@style/Biologer.ActionMode.Title</item>
    </style>

    <style name="Biologer.ActionMode.Title" parent="TextAppearance.AppCompat.Widget.ActionMode.Title">
        <item name="titleTextAppearance">@style/Theme.Biologer.Toolbar.TitleText</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>
